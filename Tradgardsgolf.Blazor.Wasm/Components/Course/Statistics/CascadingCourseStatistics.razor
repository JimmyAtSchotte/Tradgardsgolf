@using Tradgardsgolf.Contracts.Statistics
@inject IApiDispatcher apiDispatcher

<CascadingValue Value="this">
    @ChildContent
</CascadingValue>

@code {
    [CascadingParameter]
    public CascadingCourse CascadingCourse { get; set; }
    
    [Parameter]
    public RenderFragment ChildContent { get; set; }
    
    public CourseStatistic CourseStatistic { get; set; }
    public bool Loading { get; set; }

    protected override async Task OnInitializedAsync()
    {
        Loading = true;
        CourseStatistic = await apiDispatcher.Dispatch(new CourseStatisticCommand()
        {
            CourseId = CascadingCourse.Course.Id
        });
        Loading = false;
    }
}